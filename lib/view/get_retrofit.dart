import 'package:flutter/material.dart';

class GetRetrofitData extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Get Retrofit Data"),
      ),
    );
  }
}
